"use client";

import { FormEvent, useState } from "react";
import { CheckCircle2, Plus, Wrench } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";

export const NovoChamado = () => { const [chamados, setChamados] = useState<string[]>([]); const adicionar = (evento: FormEvent<HTMLFormElement>) => { evento.preventDefault(); const dados = new FormData(evento.currentTarget); setChamados((atuais) => [String(dados.get("assunto")), ...atuais]); evento.currentTarget.reset(); }; return <section className="rounded-2xl border bg-white p-5 shadow-xs"><div className="flex items-center justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[.12em] text-[#b52235]">Atendimento</p><h2 className="mt-1 text-lg font-black">Solicitar manutenção</h2></div><Dialog><DialogTrigger render={<Button />}><Plus className="size-4" /> Novo chamado</DialogTrigger><DialogContent><DialogHeader><DialogTitle>Abrir solicitação demonstrativa</DialogTitle><DialogDescription>O chamado será adicionado apenas nesta tela.</DialogDescription></DialogHeader><form onSubmit={adicionar} className="grid gap-4"><label className="grid gap-1 text-xs font-bold">Assunto<input required name="assunto" className="h-10 rounded-lg border px-3 text-sm" placeholder="Ex.: torneira com vazamento" /></label><label className="grid gap-1 text-xs font-bold">Descrição<textarea required name="descricao" className="min-h-24 rounded-lg border p-3 text-sm" /></label><Button type="submit">Adicionar à demonstração</Button></form></DialogContent></Dialog></div><div className="mt-5 grid gap-2">{chamados.map((item, indice) => <div key={`${item}-${indice}`} className="flex items-center gap-3 rounded-xl border border-emerald-200 bg-emerald-50 p-3"><CheckCircle2 className="size-4 text-emerald-600" /><span className="min-w-0 flex-1"><b className="block truncate text-xs">{item}</b><span className="text-[10px] text-emerald-700">Chamado demonstrativo adicionado</span></span></div>)}<div className="flex items-center gap-3 rounded-xl bg-[#f7f4ef] p-3"><span className="grid size-8 place-items-center rounded-lg bg-white text-[#b52235]"><Wrench className="size-4" /></span><span><b className="block text-xs">Ajuste em porta de armário</b><span className="text-[10px] text-zinc-400">Programado · exemplo visual</span></span></div></div></section>; };
