import Image from "next/image";
import { CalendarClock, CircleDollarSign, FileKey2, Home, ShieldCheck } from "lucide-react";
import { CONTRATOS_PEREZ, IMOVEIS_PEREZ } from "@/lib/perez360/dados";
import { formatarMoeda } from "@/lib/perez360/seletores";
import { CartaoDocumento } from "./cartao-documento";
import { NovoChamado } from "./novo-chamado";

export const ResumoLocatario = () => { const contrato = CONTRATOS_PEREZ[0]; const imovel = IMOVEIS_PEREZ.find((item) => item.id === contrato.imovelId)!; return <div className="grid gap-5"><section className="grid overflow-hidden rounded-2xl border bg-white shadow-xs lg:grid-cols-[18rem_1fr]"><div className="relative min-h-52"><Image src={imovel.imagens[0]} alt={imovel.titulo} fill sizes="(max-width: 1024px) 100vw, 288px" className="object-cover" /></div><div className="p-5 sm:p-7"><p className="text-[10px] font-bold uppercase tracking-[.12em] text-[#b52235]">Meu contrato</p><h2 className="mt-2 text-2xl font-black tracking-tight">{imovel.titulo}</h2><p className="mt-2 text-sm text-zinc-500">{imovel.codigo} · {imovel.bairro}, Londrina</p><div className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4">{[[FileKey2, contrato.id.toUpperCase(), "contrato"], [CalendarClock, "10/08/2026", "Próximo vencimento"], [CircleDollarSign, formatarMoeda(contrato.valor), "aluguel visual"], [ShieldCheck, "Ativo", "status demonstrativo"]].map(([Icone, valor, rotulo]) => { const Icon = Icone as typeof Home; return <div key={String(rotulo)} className="rounded-xl bg-[#f7f4ef] p-3"><Icon className="size-3.5 text-[#b52235]" /><b className="mt-3 block text-xs">{String(valor)}</b><span className="text-[9px] text-zinc-400">{String(rotulo)}</span></div>; })}</div></div></section><div className="grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-5 shadow-xs"><p className="text-[10px] font-bold uppercase tracking-[.12em] text-[#b52235]">Financeiro visual</p><h2 className="mt-1 text-lg font-black">Boletos e vencimentos</h2><div className="mt-5 grid gap-3">{[["Agosto de 2026", contrato.valor, "Vence em 10/08 · previsto"], ["Julho de 2026", contrato.valor, "Exemplo pago em 08/07"], ["Junho de 2026", contrato.valor, "Exemplo pago em 09/06"]].map(([rotulo, valor, status], indice) => <div key={String(rotulo)} className="flex items-center gap-3 rounded-xl border p-3"><span className={indice === 0 ? "grid size-8 place-items-center rounded-lg bg-amber-50 text-amber-600" : "grid size-8 place-items-center rounded-lg bg-emerald-50 text-emerald-600"}><CircleDollarSign className="size-4" /></span><span className="min-w-0 flex-1"><b className="block text-xs">{String(rotulo)}</b><span className="text-[10px] text-zinc-400">{String(status)}</span></span><b className="text-xs">{formatarMoeda(Number(valor))}</b></div>)}</div><p className="mt-4 text-[9px] text-zinc-400">Nenhum boleto real é consultado ou emitido.</p></section><section className="rounded-2xl border bg-white p-5 shadow-xs"><p className="text-[10px] font-bold uppercase tracking-[.12em] text-[#b52235]">Arquivos</p><h2 className="mt-1 text-lg font-black">Documentos e vistoria</h2><div className="mt-5 grid gap-3"><CartaoDocumento nome="Contrato de locação" detalhe="PDF · exemplo visual" /><CartaoDocumento nome="Laudo de vistoria de entrada" detalhe="PDF · exemplo visual" /><CartaoDocumento nome="Manual do locatário" detalhe="PDF · exemplo visual" /></div></section></div><NovoChamado /></div>; };
