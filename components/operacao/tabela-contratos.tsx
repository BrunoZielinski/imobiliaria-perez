import { AlertTriangle, CalendarClock, FileKey2, UserRound } from "lucide-react";
import { CONTRATOS_PEREZ, IMOVEIS_PEREZ, PESSOAS_PEREZ } from "@/lib/perez360/dados";
import { formatarMoeda } from "@/lib/perez360/seletores";

const data = (valor: string) => new Intl.DateTimeFormat("pt-BR").format(new Date(`${valor}T12:00:00-03:00`));

export const TabelaContratos = () => <div className="grid gap-3">{CONTRATOS_PEREZ.map((contrato) => { const imovel = IMOVEIS_PEREZ.find((item) => item.id === contrato.imovelId)!; const locatario = PESSOAS_PEREZ.find((item) => item.id === contrato.locatarioId)!; return <article key={contrato.id} className="grid gap-4 rounded-2xl border bg-background p-4 shadow-xs sm:grid-cols-[1.35fr_1fr_1fr_auto] sm:items-center"><div className="flex items-center gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-primary/10 text-primary"><FileKey2 className="size-4" /></span><div><p className="text-[10px] font-bold uppercase tracking-[0.1em] text-primary">{contrato.id.toUpperCase()}</p><h2 className="mt-1 text-sm font-bold">{imovel.codigo} · {imovel.bairro}</h2></div></div><div className="text-xs"><span className="flex items-center gap-1.5 text-muted-foreground"><UserRound className="size-3.5" /> Locatário</span><p className="mt-1 font-semibold">{locatario.nome}</p></div><div className="text-xs"><span className="flex items-center gap-1.5 text-muted-foreground"><CalendarClock className="size-3.5" /> Reajuste / fim</span><p className="mt-1 font-semibold">{data(contrato.proximoReajuste)} · {data(contrato.fim)}</p></div><div className="flex items-center justify-between gap-3 sm:block sm:text-right"><div><p className="text-sm font-bold">{formatarMoeda(contrato.valor)}</p><p className="text-[10px] text-muted-foreground">valor demonstrativo</p></div><span className={contrato.status === "atencao" ? "inline-flex items-center gap-1 rounded-full bg-amber-100 px-2 py-1 text-[10px] font-bold text-amber-700" : "inline-flex rounded-full bg-emerald-100 px-2 py-1 text-[10px] font-bold text-emerald-700"}>{contrato.status === "atencao" && <AlertTriangle className="size-3" />}{contrato.status === "atencao" ? "Atenção" : "Ativo"}</span></div></article>; })}</div>;
